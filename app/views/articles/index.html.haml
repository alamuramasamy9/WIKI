%h1 WIKIPEDIA - Making knowledge fun!

- @articles.each do |article|
	%h2= article.title
	%p
		Published at
		= article.created_at.strftime('%d %b, %Y')
	%p= truncate(article.content, length: 150)

= link_to "New Article", new_article_path